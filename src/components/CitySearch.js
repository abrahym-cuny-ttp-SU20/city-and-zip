import React from 'react';
import ReactDOM from 'react-dom';
import axios from 'axios';

class CitySearch extends React.Componenet {
	constructor(props){
		super(props);
	}

	render(){
		return ();
	}
}